linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt

linters-settings:
  errcheck:
    check-type-assertions: true

issues:
  exclude-dirs:
    - vendor
